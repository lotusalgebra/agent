# LOTUS Agent — Phase 1 Requirements (foundation only)

# Ollama client (for local Gemma 4)
requests>=2.31.0

# Voice — Speech-to-Text
SpeechRecognition>=3.10.0
openai-whisper>=20231117

# Voice — Text-to-Speech
edge-tts>=6.1.0

# Audio (microphone input)
# Install portaudio first: brew install portaudio
pyaudio>=0.2.13

# Clipboard + system info
pyperclip>=1.8.2
psutil>=5.9.0

# Dashboard (WebSocket server) — optional for Phase 1
websockets>=12.0

# ═══ Phase 2+ deps (install later when needed) ═══
# pyautogui>=0.9.54          # Screen control
# Pillow>=10.0.0             # Screenshots
# pytesseract>=0.3.10        # OCR (also needs: brew install tesseract)

# ═══ Phase 3 deps ═══
# resemblyzer>=0.1.3         # Voice authentication
# numpy>=1.24.0

# ═══ Phase 4 deps (content pipelines) ═══
# selenium>=4.15.0            # Browser automation (NB2, Grok)
# google-auth-oauthlib>=1.2.0 # Google Drive OAuth
# google-api-python-client>=2.100.0
